{
  "name": "unpruuf-node-mesh",
  "version": "0.1.0",
  "private": true,
  "description": "unpruuf Business Node-Mesh server. Owner-only deposit, read-only fetch, TTL-only cleanup — see NODE_MESH_SPEC.md. Structurally blind: never sees plaintext, contact identity, or which routing tags belong together.",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:tempnode": "EPHEMERAL=1 node dist/index.js",
    "dev": "ts-node src/index.ts",
    "build:exe": "node build-exe.js",
    "test": "tsc -p tsconfig.json && node --test dist/app.test.js dist/store/nodeStore.test.js dist/middleware/rateLimit.test.js dist/sqliteNativeBinding.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "esbuild": "^0.28.2",
    "postject": "1.0.0-alpha.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
