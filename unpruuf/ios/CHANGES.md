# Changes

## 2026-09-24 — ae36896
- Fixed `UnpruufApp/Podfile` deployment target: `platform :ios, '16.0'` → `'17.0'`, matching the app's documented minimum (README.md: `NavigationStack`, `VisionKit.DataScannerViewController`, `ContentUnavailableView`).

## 2026-09-24 — 7a620b6
- Added `UnpruufApp/Podfile` and 3 asset catalog `Contents.json` entries (`Assets.xcassets`, `AppIcon.appiconset`, `AccentColor.colorset`).
